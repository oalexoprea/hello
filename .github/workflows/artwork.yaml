name: Art work

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  List_Directory:
    runs-on: ubuntu-latest
    steps:
      - name: Install Cowsay
        run: |
          echo ${{ toJSON(github) }}